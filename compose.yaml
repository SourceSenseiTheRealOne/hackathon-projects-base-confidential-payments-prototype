services:
  landing:
    build: .
    image: base-confidential-payments-prototype:local
    ports:
      - "127.0.0.1:4182:4180"
    read_only: true
    tmpfs:
      - /tmp:size=32m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
